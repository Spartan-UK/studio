
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This is a temporary, wide-open rule for development.
    // It allows any request, authenticated or not.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
